name: Coffee Futures Daily Brief

on:
  schedule:
    - cron: "30 11 * * 1-5"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-brief:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Generate daily brief
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          EMAIL_TO: ${{ secrets.EMAIL_TO }}
        run: python daily_brief.py

      - name: Upload latest brief artifact
        uses: actions/upload-artifact@v4
        with:
          name: coffee-futures-daily-brief
          path: |
            output/latest.html
            output/history.json
            data/fundamentals.json

      - name: Commit generated brief
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add output/latest.html output/history.json data/fundamentals.json
          git diff --cached --quiet || git commit -m "Update coffee futures daily brief"
          git push
